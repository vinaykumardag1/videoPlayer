/**
 * Placeholder for /player/:slug route.
 * The actual player is rendered globally in App so the mini player
 * stays visible on every screen. This route keeps the URL correct.
 */
export default function PlayerRoute() {
  return null
}
